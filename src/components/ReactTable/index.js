import React from 'react';
import ReactTable from 'react-table';
import 'react-table/react-table.css';

const WrappedReactTable = props => (
  <ReactTable {...props} />
);

export default WrappedReactTable;
